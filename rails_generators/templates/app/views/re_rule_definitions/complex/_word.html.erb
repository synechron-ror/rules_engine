<% id = local_assigns[:position] ? position : 'NEW_RECORD'  %>

<div id="complex_<%=id%>">
  <%= re_text_field "Words to blacklist", "complex[#{id}][word]",  
                                            local_assigns[:word] ? word : '',
                                            :size => 40, 
                                            :required => id == 0, 
                                            :span => '4x12', 
                                            :hint => re_remove_link("Remove", "complex[#{id}]", id) %>
        
  <%= re_remove_field("complex[#{id}]", id) %>      
</div>
