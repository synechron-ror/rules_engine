<% re_rule_verify = re_rule.verify %>
<% local_first_rule = local_assigns[:first_rule] && first_rule  %>
<% local_last_rule = local_assigns[:last_rule] && last_rule  %>

<div class="span-15 last re-rule-change" id="re_rule_change_<%= re_rule.id %>">
  <div class="<%= re_rule_verify.blank? ? 're-bluebox' : 're-redbox' %> re-rule-status-<%= re_rule_verify.blank? ? 'valid' : 'verify' %>">
    <h3 class="float-left"><%= re_rule.title %></h3>
    <div class="float-right">
			<!-- NO ACTIONS ON THE RULE	 -->
    </div>
    <div class="clear"></div>    
  </div>
  <div class="<%= re_rule_verify.blank? ? 're-bluebox' : 're-redbox' %> no-top"> 
      <div class="span-10">
        <p class="top-5 left-10">
          <%= re_rule.summary %>
        </p>
      </div>
      
      <div class="prepend-10 top-10">
        <% if re_rule.re_rule_outcomes.empty? %>
          <div class="re-rule-continue">Continue</div>
        <% end %>        
        <% unless re_rule.re_rule_outcome_next.nil? %>
          <div class="re-rule-continue">Continue</div>
        <% end %>  
        <% re_rule.re_rule_outcomes_start_pipeline.each do |re_rule_outcome| %>
          <div class="re-rule-goto-pipeline"><%= re_rule_outcome.pipeline_code %></div>
        <% end %>  
        <% unless re_rule.re_rule_outcome_failure.nil? %>
          <div class="re-rule-stop-failure">Stop Failure</div>
        <% end %>  
        <% unless re_rule.re_rule_outcome_success.nil? %>
          <div class="re-rule-stop-success">Stop Success</div>
        <% end %>  
      </div>
      <div class="clear"></div>  
    </div>          
</div>

<% unless local_last_rule %>
  <div class="prepend-7 span-2"><div class="re-rule-next-change"></div></div>
<% end %>
