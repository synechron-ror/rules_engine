require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe "RulesEngine" do
  it "fails" do
    pending "******************* is it time to test the rules engine ? *********************"
  end
end
