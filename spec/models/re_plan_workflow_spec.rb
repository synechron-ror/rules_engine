require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe RePlanWorkflow do
  # look into association testing without remarkable
  # it { should belong_to(:re_plan) }
  # it { should belong_to(:re_workflow) }
end
